datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
}

model Text {
    id       Int      @id @default(autoincrement())
    filename String   @unique
    datetime DateTime
    fullText String
}
